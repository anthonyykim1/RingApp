## Closed 2026-09-12
- [2026-09-12] **#1** [status: closed] `project.yml` is AUTHORITATIVE; the committed `.xcodeproj` is a generated artifact. Settled by execution 2026-09-12: regenerated the spec into a scratch copy and diffed against the committed `project.pbxproj` — they match, UUIDs included, so nothing structural has drifted. The ONLY real gap was a missing `DEVELOPMENT_TEAM: 4RXYGKM63F`, which meant any `xcodegen generate` produced a project that could not sign for a device; two lines added to the spec, verified to close the gap exactly. Three cosmetic deltas remain on regeneration (`explicitFileType` -> `lastKnownFileType` on the two product refs, plus an added `TargetAttributes` block) — harmless, but the committed `.xcodeproj` was deliberately NOT regenerated, so pair the first regeneration with a device build. Recorded as an explicit delta in `CLAUDE.md`.

- [2026-09-12] **#2** [status: closed] RingApp is now harness-covered. `harness init` run, the deliberately-failing placeholder replaced with a real `ios_build` gate, and red -> green confirmed: `[FAIL] placeholder` -> `[OK] ios_build (7.9s)` from cold. The gate compiles BOTH targets (the built `RingApp.app` was confirmed to contain `PlugIns/NotificationService.appex`) and was verified to actually FAIL on broken Swift, in a throwaway worktree, so it is not a gate that cannot go red. It deliberately does not run xcodegen (not a no-op here) and uses `-sdk iphonesimulator` to keep the command apostrophe-free for shlex. `[prompt]` carries the do-not-self-verify sentence and the BLE warning; `[disclosure]` states the gate is compile-only and BLE-blind, with per-area notes for `RingApp/BLE/**` and `NotificationService/**`. ⚠️ `.swift` is a guarded extension, so Swift changes here now require a ticket.
